AccountManagementServiceAutoChargeStatus:
  type: string
  nullable: true
  enum:
  - NONE
  - ACTIVE
  - PAUSED
  - UNKNOWN
  description: |
    <div lang="ja">AccountManagementServiceAutoChargeStatusは、自動入金状態を表します。<br>このフィールドは、レスポンスの際に返却されますが、リクエストの際には無視されます。</div>
    <div lang="en">AccountManagementServiceAutoChargeStatus displays the auto charge status.<br>Although this field will be returned in the response, it will be ignored on input.</div>
    <hr>
    <p>* <code>NONE</code> - <span lang="ja">なし</span><span lang="en">none</span></p>
    <p>* <code>ACTIVE</code> - <span lang="ja">有効</span><span lang="en">active</span></p>
    <p>* <code>PAUSED</code> - <span lang="ja">無効</span><span lang="en">paused</span></p>
    <p>* <code>UNKNOWN</code> - <span lang="ja">未知の値です。</span><span lang="en">Unknown Value</span></p>